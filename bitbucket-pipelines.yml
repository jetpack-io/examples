pipelines:
  branches: 
      main: 
          - step:
                  name: release
                  script:
                      - curl -fsSL https://get.jetpack.io | bash -s -- --yes
                      - INTERNAL=1 jetpack release -w $PROD_WORKSPACE -s api.dev.jetpack.io